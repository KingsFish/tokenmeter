---
phase: 01-plugin-marketplace-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/
  - README.md
autonomous: true
requirements: [MKT-02, MKT-04]
user_setup:
  - service: GitHub
    why: "Push code to GitHub repository"
    env_vars: []
    dashboard_config:
      - task: "Verify GitHub account can push to KingsFish/tokenmeter"
        location: "GitHub.com repository settings"

must_haves:
  truths:
    - "GitHub repository KingsFish/tokenmeter contains all plugin files"
    - "Remote main branch is up-to-date with local"
    - "README.md accurately reflects current installation process"
  artifacts:
    - path: "GitHub repository KingsFish/tokenmeter"
      provides: "Public plugin repository"
      contains: "main branch with all commits"
    - path: "README.md"
      provides: "Installation documentation"
      min_lines: 300
  key_links:
    - from: "Local main branch"
      to: "GitHub KingsFish/tokenmeter"
      via: "git push"
      pattern: "git push origin main"
---

<objective>
Push plugin code to GitHub repository and verify README documentation accuracy.

Purpose: Ensure the plugin repository is accessible on GitHub and documentation accurately describes the installation process.

Output: Updated GitHub repository with all code pushed, verified README documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Current State

The local repository is configured with remote `origin` pointing to `git@github.com:KingsFish/tokenmeter.git`. The local main branch is ahead of origin/main by 5 commits. The README contains installation instructions for marketplace installation, but needs verification.

## Key Files

- `.claude-plugin/plugin.json` - Plugin metadata
- `.claude-plugin/marketplace.json` - Marketplace registration
- `README.md` - User-facing documentation
- `commands/*.md` - Claude Code command definitions
- `scripts/*.sh` - Implementation scripts
- `dashboard/*` - Web dashboard files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push code to GitHub</name>
  <files>.git/</files>
  <read_first>
    - Current git status
    - Remote configuration
    - Branch state
  </read_first>
  <behavior>
    - All local commits on main branch are pushed to origin/main
    - Remote branch matches local branch
    - No uncommitted changes in tracked files (except .planning/)
  </behavior>
  <action>
    1. Verify git remote is correctly configured as `git@github.com:KingsFish/tokenmeter.git`
    2. Add and commit any necessary changes (excluding .planning/ directory)
    3. Push all commits from local main branch to origin/main
    4. Verify push was successful by checking remote branch status

    Commands to execute:
    ```bash
    # Check current status
    git status

    # Push to GitHub (if no uncommitted changes in non-.planning files)
    git push origin main

    # Verify remote state
    git log --oneline origin/main -5
    ```

    If there are uncommitted changes in plugin files (not .planning/), commit them first with message "chore: prepare for marketplace publishing".
  </action>
  <verify>
    <automated>git log --oneline origin/main | head -1 | grep -q "." && echo "Push verified"</automated>
  </verify>
  <done>
    - Local main branch is pushed to origin/main
    - `git log --oneline origin/main -5` shows recent commits
    - GitHub repository KingsFish/tokenmeter is accessible
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and update README documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md (current version)
    - .claude-plugin/plugin.json (actual plugin name and metadata)
    - .claude-plugin/marketplace.json (marketplace name)
    - commands/*.md (actual command arguments and usage)
  </read_first>
  <behavior>
    - README Installation section correctly references marketplace name "tokenmeter-marketplace"
    - All command examples match actual command files
    - Plugin name matches plugin.json
    - No outdated information
  </behavior>
  <action>
    Verify README.md accuracy against actual plugin configuration:

    1. **Verify marketplace name**: README should reference "tokenmeter-marketplace" marketplace with "tokenmeter" plugin (from marketplace.json)

    2. **Verify installation commands**:
       - Check if `claude marketplace add KingsFish/tokenmeter` is correct (this adds the marketplace)
       - Check if `claude plugin install tokenmeter` is correct (this installs the plugin)

    3. **Verify command usage**:
       - Check `/usage` command arguments match commands/usage.md
       - Check `/usage-dashboard` command arguments match commands/usage-dashboard.md
       - Check `/usage-config-price` command arguments match commands/usage-config-price.md

    4. **Update if needed**: If any discrepancies found, update README.md to match actual implementation.

    Key checks from plugin.json and marketplace.json:
    - marketplace.json has `"name": "tokenmeter-marketplace"`
    - marketplace.json has plugin entry with `"name": "tokenmeter"`
    - plugin.json has `"name": "tokenmeter"`

    Current README shows:
    ```bash
    # Add the marketplace
    claude marketplace add KingsFish/tokenmeter

    # Install the plugin
    claude plugin install tokenmeter
    ```

    This appears correct but verify against Claude Code documentation if possible.
  </action>
  <verify>
    <automated>grep -q "claude marketplace add KingsFish/tokenmeter" README.md && grep -q "claude plugin install tokenmeter" README.md && echo "README verified"</automated>
  </verify>
  <done>
    - README.md marketplace installation commands are correct
    - Command usage examples match actual command files
    - No references to outdated or incorrect plugin names
    - Documentation accurately reflects current implementation
  </done>
</task>

</tasks>

<verification>
1. GitHub repository accessible at `https://github.com/KingsFish/tokenmeter`
2. All commits visible in repository
3. README.md correctly describes installation process
4. Plugin files (commands/, scripts/, dashboard/, .claude-plugin/) present in repository
</verification>

<success_criteria>
- [ ] Git push to origin/main succeeds
- [ ] `git log origin/main` shows all expected commits
- [ ] README.md contains correct marketplace name "tokenmeter-marketplace"
- [ ] README.md contains correct plugin name "tokenmeter"
- [ ] Command examples in README match actual command files
- [ ] No uncommitted plugin files in local repository
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-marketplace-publishing/01-01-SUMMARY.md`
</output>
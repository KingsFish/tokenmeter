---
phase: 01-plugin-marketplace-publishing
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: []
autonomous: false
requirements: [MKT-01, MKT-03]
user_setup: []

must_haves:
  truths:
    - "User can add marketplace via 'claude marketplace add KingsFish/tokenmeter'"
    - "User can install plugin via 'claude plugin install tokenmeter'"
    - "All three commands (/usage, /usage-dashboard, /usage-config-price) work after install"
    - "Installation works on fresh Claude Code setup"
  artifacts:
    - path: "~/.claude/plugins/cache/tokenmeter-marketplace/"
      provides: "Installed plugin cache"
      contains: "Plugin files after installation"
    - path: "~/.claude/marketplaces/"
      provides: "Marketplace registration"
      contains: "tokenmeter-marketplace entry"
  key_links:
    - from: "Claude Code CLI"
      to: "GitHub repository"
      via: "claude marketplace add"
      pattern: "claude marketplace add KingsFish/tokenmeter"
    - from: "Claude Code CLI"
      to: "Installed plugin"
      via: "claude plugin install tokenmeter"
      pattern: "claude plugin install tokenmeter"
---

<objective>
Test the complete marketplace installation flow and validate all plugin commands work correctly.

Purpose: Verify that users can successfully install TokenMeter via the Claude Code marketplace and that all plugin functionality works as expected.

Output: Verified installation process, tested commands, documented any issues or adjustments needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Prerequisites

Plan 01-01 must complete successfully:
- GitHub repository KingsFish/tokenmeter is accessible
- All code pushed to main branch
- README documentation verified

## Plugin Commands to Test

Based on commands/ directory:
1. `/usage` - CLI usage summary (commands/usage.md)
2. `/usage-dashboard` - Web dashboard (commands/usage-dashboard.md)
3. `/usage-config-price` - Price configuration (commands/usage-config-price.md)

## Expected Behavior

After `claude marketplace add KingsFish/tokenmeter`:
- Marketplace registered in ~/.claude/marketplaces/

After `claude plugin install tokenmeter`:
- Plugin installed in ~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/
- Commands available in Claude Code session
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test marketplace addition</name>
  <files></files>
  <read_first>
    - .claude-plugin/marketplace.json (marketplace configuration)
    - .claude-plugin/plugin.json (plugin metadata)
  </read_first>
  <behavior>
    - `claude marketplace add KingsFish/tokenmeter` command succeeds
    - Marketplace appears in `claude marketplace list`
    - Plugin "tokenmeter" visible in marketplace
  </behavior>
  <action>
    Test the marketplace addition process:

    1. **Add marketplace**:
       ```bash
       claude marketplace add KingsFish/tokenmeter
       ```

    2. **Verify marketplace registration**:
       ```bash
       claude marketplace list
       ```

       Expected output should include:
       - tokenmeter-marketplace or KingsFish/tokenmeter
       - Shows tokenmeter plugin in the list

    3. **Check marketplace details**:
       ```bash
       claude marketplace show tokenmeter-marketplace
       ```

       Or check the marketplace file:
       ```bash
       cat ~/.claude/marketplaces/tokenmeter-marketplace.json
       ```

    Expected results:
    - Command succeeds without errors
    - Marketplace is registered
    - Plugin list shows "tokenmeter" plugin
    - Plugin metadata matches .claude-plugin/plugin.json
  </action>
  <verify>
    <automated>claude marketplace list | grep -q "tokenmeter" && echo "Marketplace added successfully"</automated>
  </verify>
  <done>
    - Marketplace KingsFish/tokenmeter successfully added
    - `claude marketplace list` shows tokenmeter-marketplace
    - Plugin "tokenmeter" available for installation
  </done>
</task>

<task type="auto">
  <name>Task 2: Install and test plugin</name>
  <files></files>
  <read_first>
    - commands/usage.md (expected /usage command behavior)
    - commands/usage-dashboard.md (expected /usage-dashboard command behavior)
    - commands/usage-config-price.md (expected /usage-config-price command behavior)
  </read_first>
  <behavior>
    - `claude plugin install tokenmeter` succeeds
    - Plugin appears in `claude plugin list`
    - All three commands work in Claude Code session
  </behavior>
  <action>
    Install the plugin and verify basic functionality:

    1. **Install plugin**:
       ```bash
       claude plugin install tokenmeter
       ```

    2. **Verify installation**:
       ```bash
       claude plugin list
       ```

       Expected: tokenmeter should appear in the list with status "installed" or similar.

    3. **Check plugin cache**:
       ```bash
       ls -la ~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/
       ```

       Expected directories and files:
       - commands/usage.md
       - commands/usage-dashboard.md
       - commands/usage-config-price.md
       - scripts/parse-usage.sh
       - scripts/start-dashboard.sh
       - scripts/price-config.sh
       - dashboard/
       - .claude-plugin/plugin.json
       - .claude-plugin/marketplace.json

    4. **Test /usage command** (in Claude Code session):
       Start a Claude Code session and run:
       ```
       /usage
       ```

       Expected: Token usage summary displayed (or "no sessions found" if no data).

    5. **Test /usage-config-price command** (in Claude Code session):
       ```
       /usage-config-price --list
       ```

       Expected: Current price configuration displayed.

    6. **Test /usage-dashboard command** (in Claude Code session):
       ```
       /usage-dashboard 8765
       ```

       Expected: Dashboard server starts, browser may open automatically.

    Note: If no Claude Code sessions exist yet, commands should handle gracefully (show empty stats or appropriate message).

    **Script permissions check**:
    ```bash
    ls -l ~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/scripts/*.sh
    ```

    All .sh files should be executable. If not:
    ```bash
    chmod +x ~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/scripts/*.sh
    ```
  </action>
  <verify>
    <automated>claude plugin list | grep -q "tokenmeter" && test -x ~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/scripts/parse-usage.sh && echo "Plugin installed and executable"</automated>
  </verify>
  <done>
    - Plugin tokenmeter successfully installed
    - `claude plugin list` shows tokenmeter
    - Plugin files present in cache directory
    - Script files are executable
    - `/usage` command works (returns output or appropriate message)
    - `/usage-config-price --list` works
    - `/usage-dashboard` starts without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify installation and command functionality</name>
  <files></files>
  <action>Human verification checkpoint - no automated action required. User will manually test all commands and verify functionality.</action>
  <verify>User types "approved" after completing verification steps</verify>
  <done>User confirms all commands work correctly or describes issues to fix</done>
  <what-built>
    Marketplace registration and plugin installation complete. All three commands tested.
  </what-built>
  <how-to-verify>
    1. **Verify marketplace is added**:
       - Run: `claude marketplace list`
       - Confirm "tokenmeter-marketplace" or "KingsFish/tokenmeter" appears

    2. **Verify plugin is installed**:
       - Run: `claude plugin list`
       - Confirm "tokenmeter" appears in the list

    3. **Test each command in Claude Code session**:
       - Start Claude Code: `claude`
       - Run: `/usage`
         - Expected: Shows token usage summary (or "no sessions" message)
       - Run: `/usage-config-price --list`
         - Expected: Shows price configuration table
       - Run: `/usage-dashboard 8765`
         - Expected: Dashboard server starts at http://localhost:8765
         - Open browser to verify dashboard loads

    4. **Verify dashboard functionality** (if you have session data):
       - Dashboard shows token usage charts
       - Sessions table displays correctly
       - Settings page accessible
       - No console errors in browser

    5. **Test on clean setup (optional but recommended)**:
       - Use a different user account or test machine
       - Run `claude marketplace add KingsFish/tokenmeter`
       - Run `claude plugin install tokenmeter`
       - Test all three commands

    Type "approved" if all commands work correctly, or describe any issues encountered.
  </how-to-verify>
  <resume-signal>Type "approved" if installation and commands work correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Marketplace successfully added via `claude marketplace add KingsFish/tokenmeter`
2. Plugin successfully installed via `claude plugin install tokenmeter`
3. All three commands (`/usage`, `/usage-dashboard`, `/usage-config-price`) work correctly
4. Plugin files correctly placed in `~/.claude/plugins/cache/tokenmeter-marketplace/tokenmeter/`
5. Script files are executable
</verification>

<success_criteria>
- [ ] `claude marketplace add KingsFish/tokenmeter` succeeds
- [ ] `claude marketplace list` shows tokenmeter-marketplace
- [ ] `claude plugin install tokenmeter` succeeds
- [ ] `claude plugin list` shows tokenmeter
- [ ] Plugin cache directory exists with all files
- [ ] Script files (.sh) are executable
- [ ] `/usage` command executes without errors
- [ ] `/usage-config-price --list` executes without errors
- [ ] `/usage-dashboard` starts server successfully
- [ ] Dashboard loads in browser without errors
- [ ] Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-marketplace-publishing/01-02-SUMMARY.md`
</output>
---
task_id: quick-20260402-093029
type: quick
status: in_progress
created: 2026-04-02T09:30:29Z
---

# Quick Task: 项目筛选改为下拉框

## Objective

将前端 Dashboard 页面上的项目筛选从文本输入框改为下拉选择框，与模型筛选保持一致的 UI 风格。

## Changes Required

### 1. HTML (index.html)

将 `filter-project` 从 input 改为 select：
```html
<!-- 当前 -->
<input type="text" class="param-input param-input--text" id="filter-project">

<!-- 改为 -->
<select class="param-select" id="filter-project">
    <option value="" data-i18n="allProjects">全部项目</option>
</select>
```

### 2. JavaScript (app.js)

- 添加 `availableProjects` 全局变量
- 添加 `populateProjectDropdown()` 函数
- 在 `initDashboard()` 中填充项目下拉框
- 修改事件监听器：从 input debounce 改为 change

### 3. i18n (i18n.js)

- 添加 `allProjects` 翻译项（zh: "全部项目", en: "All Projects")
- 移除 `searchProject`（不再需要）

## Execution Plan

1. 更新 index.html - 改为 select 元素
2. 更新 i18n.js - 添加 allProjects
3. 更新 app.js - 添加变量、函数、事件监听
4. Git commit
5. 更新 STATE.md